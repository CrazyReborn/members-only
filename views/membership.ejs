<!DOCTYPE html>
<html>
  <head>
    <title>Become a member</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
      <a href='/'>Home</a>
    </header>
    <h1>Become a Member!</h1>
    <div>
        <form action='' method='POST'>
            <label for='secret'>Enter a secret password to join:</label>
            <input type='text' name='secret-password' required=true />
            <input type='submit'/>
        </form>
        <% if (typeof errors !== 'undefined') { %>
            <% for (let i=0; i < errors.length; i++) { %>
                <p><%= errors[i].msg %></p>
            <%} %>
        <% } %>
    </div>
  </body>
</html>
